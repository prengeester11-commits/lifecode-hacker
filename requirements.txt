flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1
openai==1.57.0
requests==2.32.3
korean-lunar-calendar==0.3.1
gunicorn==22.0.0
ephem==4.1.6
Pillow==10.4.0
# weasyprint / xhtml2pdf 제거: PDF 생성을 끄면서 불필요해졌고,
# Render 무료 빌드에서 설치 실패(exit 1)의 원인이 되어 제외함.
# (PDF가 다시 필요하면 이 둘을 복원하고 ENABLE_PDF=true 설정)
